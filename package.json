{
	"name": "pnw-sticker-block",
	"description": "A sticker block for placing images on the page",
	"dependencies": {
		"@wordpress/scripts": "^18.0.1"
	},
	"scripts": {
		"build": "wp-scripts build src/*.js --output-path=build/"
	}
}
